#include <stdio.h>
#include <cstdint>
//#include "compactador.cpp"

/*
Formato do cabeçalho:
    > 1 byte, numFolhas;
    > 1 byte, num de bits a ler, da direita p esquerda.
    > Código de cada caractere;
    > Sequência de bits do arquivo compactado.
*/

class Descompactador {
    private:

    public:

};